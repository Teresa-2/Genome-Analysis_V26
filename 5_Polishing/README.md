**Note on Pilon scripts**
This step was executed locally (`run_pilon.sh`) during a server downtime period to maintain project continuity, rather than on the HPC cluster.
Since the polishing step is computationally intensive and time-consuming, the computation was not repeated.
Both a local (`run_pilon.sh`) and a SLURM-compatible (`run_pilon_slurm.sh`) version are provided for reference and future reproducibility.
Note: Execution logs were not retained form the local run

**Note on Quast scripts**
This step was executed locally (`quast_polished-assembly.sh`) during a server downtime period to mantain project continuity, rather than on the HPC cluster.
Both a local (`quast_polished-assembly.sh`) and a SLURM-compatible (`slurm-quast_polished-assembly.sh`)  version are provided for reference and future reproducibility.
Note: Execution logs were not retained form the local run

**Note on Busco scripts**
This step was executed locally (`busco_polished-assembly.sh`) during a server downtime period to mantain project continuity, rather than on the HPC cluster.
Both a local (`busco_polished-assembly.sh`) and a SLURM-compatible (`slurm-busco_polished-assembly.sh`)  version are provided for reference and future reproducibility.
Note: Execution logs were not retained form the local run
